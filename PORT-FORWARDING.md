# 🌐 Инструкция по проброс портов в VS Code

## 📋 Используемые порты

- **API (Backend)**: `3000`
- **Web (Frontend)**: `3001`
- **Admin (Dashboard)**: `3002`

## 🚀 Быстрый старт

### 1. Запустите все сервисы

Откройте 3 терминала и выполните:

```bash
# Терминал 1 - API
pnpm dev:api

# Терминал 2 - Web
pnpm dev:web

# Терминал 3 - Admin
pnpm dev:admin
```

### 2. Настройте Port Forwarding в VS Code

#### Способ 1: Через UI (рекомендуется)

1. Нажмите `Cmd+Shift+P` (Mac) или `Ctrl+Shift+P` (Windows)
2. Введите: `View: Toggle Ports`
3. В панели PORTS нажмите иконку **+** (Forward a Port)
4. Добавьте порты: `3000`, `3001`, `3002`
5. Для каждого порта: ПКМ → **Port Visibility** → **Public**

#### Способ 2: Через settings.json

Добавьте в `.vscode/settings.json`:

```json
{
  "remote.autoForwardPorts": false,
  "remote.portsAttributes": {
    "3000": {
      "label": "API",
      "onAutoForward": "notify",
      "protocol": "https"
    },
    "3001": {
      "label": "Web",
      "onAutoForward": "notify",
      "protocol": "https"
    },
    "3002": {
      "label": "Admin",
      "onAutoForward": "notify",
      "protocol": "https"
    }
  }
}
```

### 3. Скопируйте проброшенные URL

В панели PORTS найдите столбец **Forwarded Address**:
- API: `https://xxx-3000.app.github.dev`
- Web: `https://xxx-3001.app.github.dev`
- Admin: `https://xxx-3002.app.github.dev`

### 4. Обновите конфигурацию

#### Автоматически (рекомендуется):

```bash
./update-forwarded-urls.sh
```

Скрипт запросит URL для API и автоматически обновит `.env.local` файлы.

#### Вручную:

**apps/web/.env.local:**
```env
NEXT_PUBLIC_API_URL=https://xxx-3000.app.github.dev
```

**apps/admin/.env.local:**
```env
NEXT_PUBLIC_API_URL=https://xxx-3000.app.github.dev
```

### 5. Перезапустите фронтенд

```bash
# Остановите (Ctrl+C) и перезапустите:
pnpm dev:web
pnpm dev:admin
```

### 6. Поделитесь ссылками

Теперь можете делиться:
- 🌐 **Web сайт**: `https://xxx-3001.app.github.dev`
- 🔧 **Админка**: `https://xxx-3002.app.github.dev`
- 📚 **API Docs**: `https://xxx-3000.app.github.dev/api/docs`

## 🔍 Проверка работоспособности

### Проверка API:
```bash
curl https://xxx-3000.app.github.dev/api/docs
```

### Проверка в браузере:
1. Откройте Web URL в браузере
2. Откройте DevTools (F12) → Network
3. Проверьте, что запросы к API идут на проброшенный URL

## ⚠️ Возможные проблемы

### 1. CORS ошибки

**Симптом**: `Access to fetch has been blocked by CORS policy`

**Решение**: CORS уже настроен в `services/api/src/main.ts`. Если проблема сохраняется, проверьте что:
- API запущен
- URL в `.env.local` правильный (без `/` в конце)

### 2. API не доступен из фронтенда

**Решение**:
1. Проверьте что порт 3000 проброшен и публичный
2. Проверьте `.env.local` файлы
3. Перезапустите фронтенд после изменения `.env.local`

### 3. Сессии/Куки не работают

**Решение**: Убедитесь что все URL используют HTTPS (не HTTP)

### 4. Порты автоматически меняются

**Решение**: Закрепите порты в панели PORTS (ПКМ → **Set Label**)

## 📝 Дополнительные команды

### Проверка запущенных портов:
```bash
lsof -i :3000
lsof -i :3001
lsof -i :3002
```

### Остановка всех процессов:
```bash
lsof -ti :3000,:3001,:3002 | xargs kill
```

### Просмотр логов:
```bash
# API логи
pnpm --filter api start:dev

# Web логи с подробностями
pnpm --filter web dev --debug
```

## 🔒 Безопасность

⚠️ **Важно**: Проброшенные порты становятся **публично доступными**!

- Не используйте реальные данные
- Не храните секреты в `.env.local`
- Закрывайте порты после демонстрации
- Используйте временные тестовые аккаунты

## 🛠️ Альтернативы

Если VS Code Port Forwarding не работает, попробуйте:

1. **ngrok**: `ngrok http 3000`
2. **LocalTunnel**: `lt --port 3000`
3. **Cloudflare Tunnel**: `cloudflared tunnel`

---

**Создано**: 2025-12-15
**Документация**: [VS Code Port Forwarding](https://code.visualstudio.com/docs/editor/port-forwarding)
