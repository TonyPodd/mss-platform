# Конфигурация для Admin панели
name: mss-admin
type: web

build:
  installCommand: pnpm install
  buildCommand: |
    cd apps/admin
    pnpm build

run:
  startCommand: cd apps/admin && pnpm start

env:
  NODE_ENV: production
  PORT: 3000

resources:
  cpu: 0.5
  memory: 512Mi
